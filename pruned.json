{
  "note": "Operator-promoted 2026-07-07: modifier_edge_report n=8225, Bonferroni alpha=0.0026, the 3 significant HURTS verdicts (NOISE stays live until significant). Operator-promoted 2026-07-09: strategy_edge_analyzer.py per-strategy net-R, n>=200 AND gross mean < -0.03% (genuinely negative before the flat cost drag, not just cost-taxed-to-negative) -- 5 of these 8 independently matched daily_report.json's rolling-win-rate-based pause_candidates. price_structure was in that pause list too but excluded here: its gross mean is +0.053%, positive -- its net-of-cost figure is only negative from the flat cost subtraction, not a bad signal. Reversible: remove a name and restart. Operator-promoted 2026-07-15: modifier_edge_report n=15434, Bonferroni alpha=0.001852 -- expiry_mod, weinstein_mod, pivot_boss_mod, nr_mod, volume_mod, market_profile_mod all verdict=NOISE (no significant endorsed-vs-silent lift, temporal-stability check does not apply since they never reached significance). ai_score deliberately excluded even though flagged NOISE: it is not an additive score modifier in signal_engine.py (no _cand_meta/_sig_meta key), just a logged copy of signal.confidence -- pruning it would be a no-op. Reversible: remove a name and restart. Operator-promoted 2026-08-05: validation_harness.py walk-forward, 66 dev windows (deepest available, spanning the corrected/re-verified data-fetch path), supertrend_mtf dev_avg_sharpe=-1.3246, avg_trades/window=28.1 (below the 30-trade minimum on top of being negative), verdict=FAIL. Confirmed live-registered under this exact label (run_supertrend_mtf_strategy returns strategy='supertrend_mtf' in signal_engine.py), so this is a real gate, not a no-op like ai_score. cpr and vwap_reversion showed FAIL the same day but were NOT promoted here: only a single shallow 6-window run each so far (cpr sharpe=-3.0, vwap_reversion 0 trades/window entirely -- a doesn't-fire question, not a proven-negative one), below the evidentiary bar used for every other entry in this file. ema_5min was considered and explicitly excluded: it has no live signal_engine.py registration under any name (checked STRATEGIES list and all run_*_strategy definitions) -- pruning it would be a no-op, same reasoning as the existing ai_score exclusion. Reversible: remove a name and restart.",
  "strategies": [
    "alligator_ao",
    "vrvp_zone",
    "holy_grail",
    "chart_pattern_double_top",
    "chart_pattern_inverse_head_shoulders",
    "rsi_divergence",
    "chart_pattern_descending_triangle",
    "trend",
    "supertrend_mtf"
  ],
  "modifiers": [
    "cross_asset_mod",
    "mtf_pivot_mod",
    "sr_level_mod",
    "whale_mod",
    "expiry_mod",
    "weinstein_mod",
    "pivot_boss_mod",
    "nr_mod",
    "volume_mod",
    "market_profile_mod"
  ],
  "whale_mod_note": "2026-07-10: constant -1.0 on all 10,717 signals over 10 days. Root cause: the FII-futures component signals BEARISH whenever fut_long/fut_short < 0.8, but FII index futures are chronically net short (hedging books) - live ratio 0.106, so the condition is true essentially every day. A permanent -1 offset carries zero information, and participant-OI signals were already measured to have NO daily edge (participant-oi-no-edge). Value still computed+logged for measurement; only its score contribution is removed."
}
