# pattern_engine dependencies (all already used elsewhere in the platform)
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3   # RANSAC trendline fitting (degrades gracefully if absent)
# optional, only for pattern_engine.visualization (not imported by the core):
# matplotlib>=3.7
