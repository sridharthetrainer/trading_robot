[pytest]
# These are interactive diagnostics, not tests.  They authenticate against
# live services and historically ran at import time during ``pytest``
# collection.  Keep them runnable directly while making the default suite
# deterministic and offline-safe.
addopts =
    --ignore=test_angel_direct.py
    --ignore=test_data.py
    --ignore=test_all_files.py
